# Production Environment Variables for Vercel
# Copy these to your Vercel project settings

NODE_ENV=production
CORS_ORIGIN=https://your-frontend-domain.vercel.app
JWT_SECRET=your-super-secure-jwt-secret-at-least-32-characters-long
JWT_EXPIRES_IN=7d
LOG_LEVEL=info
MAX_UPLOAD_MB=10

# Database Configuration
# ⚠️ Important: SQLite doesn't work well with Vercel serverless
# Consider using one of these cloud databases:
# DB_URL=your-cloud-database-url

# Examples:
# Turso (SQLite-compatible): libsql://your-database.turso.io?authToken=your-token
# PlanetScale (MySQL): mysql://user:pass@host:port/database?sslaccept=strict
# Neon (PostgreSQL): postgresql://user:pass@host:port/database?sslmode=require
# Supabase (PostgreSQL): postgresql://postgres:pass@host:port/postgres

# File Storage Configuration
# ⚠️ Important: Local storage doesn't persist on Vercel
# Consider using cloud storage for production:
# STORAGE_PROVIDER=vercel-blob
# VERCEL_BLOB_READ_WRITE_TOKEN=your-token
